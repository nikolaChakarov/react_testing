import styled from "styled-components";

const Form = () => {
    return (
        <FormContainer>
            <form></form>
        </FormContainer>
    );
};

const FormContainer = styled.div``;

export default Form;
