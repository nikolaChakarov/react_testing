import styled from "styled-components";

const Home = () => {
    return (
        <HomeContainer>
            <h3>Home</h3>
        </HomeContainer>
    );
};

const HomeContainer = styled.div``;

export default Home;
